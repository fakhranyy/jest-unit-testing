describe("Setup Functions", () => {
  beforeAll(() => {
    // * it's run once before all tests
    // connectToBD();
  });

  afterAll(() => {
    //* it's run once after all tests finish
    // disConnect();
  });

  beforeEach(() => {
    //* runs once before every test
  });

  afterEach(() => {
    //*  runs once after every test
  });
  test("test 1", () => {});
  test("test 2", () => {});
  test("test 3", () => {});
  test("test 4", () => {});
});
